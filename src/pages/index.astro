---
const sports = [
  { slug: "baseball", name: "Baseball", emoji: "⚾" },
  { slug: "basketball", name: "Basketball", emoji: "🏀" },
  { slug: "football", name: "Football", emoji: "🏈" },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Card Vault</title>
  </head>

  <body class="min-h-screen bg-zinc-950 text-zinc-100">
    <main class="mx-auto max-w-6xl px-4 py-12">
      <header class="mb-10">
        <div class="inline-flex items-center gap-2 rounded-full bg-zinc-900/60 px-4 py-2 ring-1 ring-zinc-800">
          <span class="text-sm text-zinc-200">Public Card Gallery</span>
        </div>
        <h1 class="mt-5 text-4xl font-semibold tracking-tight">
          BradyRip Card Vault
        </h1>
        <p class="mt-3 max-w-2xl text-zinc-300">
          Choose a sport, then choose a set. No database—this site updates automatically based on your folders and images.
        </p>
      </header>

      <section class="grid gap-5 md:grid-cols-3">
        {sports.map((s) => (
          <a
            href={`/${s.slug}/`}
            class="group rounded-3xl bg-zinc-950 ring-1 ring-zinc-800 hover:ring-zinc-600 transition overflow-hidden"
          >
            <div class="p-6">
              <div class="text-4xl">{s.emoji}</div>
              <div class="mt-4 text-xl font-semibold">{s.name}</div>
              <div class="mt-1 text-sm text-zinc-300">Browse sets →</div>
            </div>
            <div class="h-1 bg-gradient-to-r from-zinc-700 via-zinc-500 to-zinc-700 opacity-0 group-hover:opacity-100 transition"></div>
          </a>
        ))}
      </section>

      <footer class="mt-12 text-sm text-zinc-500">
        Tip: Add images under <code class="rounded bg-zinc-900 px-2 py-1 ring-1 ring-zinc-800">public/cards/&lt;sport&gt;/&lt;set&gt;</code> and push.
      </footer>
    </main>
  </body>
</html>

---
// src/pages/index.astro
const sports = [
  { slug: "baseball", label: "Baseball", icon: "⚾" },
  { slug: "basketball", label: "Basketball", icon: "🏀" },
  { slug: "football", label: "Football", icon: "🏈" },
];

// This timestamp helps you confirm Cloudflare is serving the latest build
const buildStamp = new Date().toISOString();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Card Gallery</title>
  </head>

  <body class="min-h-screen bg-zinc-950 text-zinc-100">
    <main class="mx-auto max-w-6xl px-4 py-12">
      <div class="relative overflow-hidden rounded-3xl ring-1 ring-zinc-800 bg-gradient-to-b from-zinc-900/50 to-zinc-950 p-8">
        <div class="absolute -top-24 -right-12 h-64 w-64 rounded-full bg-indigo-500/10 blur-3xl"></div>
        <div class="absolute -bottom-20 -left-10 h-64 w-64 rounded-full bg-emerald-500/10 blur-3xl"></div>

        <div class="inline-flex items-center gap-2 rounded-full bg-zinc-900/60 ring-1 ring-zinc-800 px-3 py-1 text-xs text-zinc-200">
          ✨ Public Card Gallery
        </div>

        <h1 class="mt-4 text-4xl sm:text-5xl font-semibold tracking-tight">
          Your collection, beautifully browsable.
        </h1>
        <p class="mt-3 text-zinc-300 max-w-2xl">
          Pick a sport → pick a set → view cards. Add images to folders and push to publish.
        </p>

        <div class="mt-4 text-xs text-zinc-500">
          Build: {buildStamp}
        </div>
      </div>

      <section class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {sports.map((s) => (
          <a
            href={`/${s.slug}/`}
            class="group relative overflow-hidden rounded-3xl bg-zinc-950 ring-1 ring-zinc-800 hover:ring-zinc-600 transition p-6"
          >
            <div class="absolute -top-10 -right-8 text-[120px] opacity-[0.08] select-none">
              {s.icon}
            </div>

            <div class="text-sm text-zinc-300">{s.icon} {s.label}</div>
            <div class="mt-2 text-2xl font-semibold tracking-tight">
              Browse sets →
            </div>
            <p class="mt-2 text-zinc-400">
              View set covers and card counts.
            </p>
            <div class="mt-4 inline-flex items-center rounded-xl bg-zinc-900/60 ring-1 ring-zinc-800 px-3 py-2 text-sm group-hover:ring-zinc-600">
              Open {s.label}
            </div>
          </a>
        ))}
      </section>

      <div class="mt-6 rounded-2xl ring-1 ring-zinc-800 bg-zinc-950 p-6 text-zinc-300">
        <div class="text-xs uppercase tracking-widest text-zinc-400">Tip</div>
        <div class="mt-2">
          Add images under{" "}
          <code class="rounded bg-zinc-900 px-2 py-1 ring-1 ring-zinc-800">
            public/cards/&lt;sport&gt;/&lt;set&gt;/
          </code>{" "}
          then push to publish.
        </div>
      </div>
    </main>
  </body>
</html>
